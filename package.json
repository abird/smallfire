{
  "name": "smallfire",
  "version": "1.0.0",
  "description": "Very small Firebase Javascript client",
  "main": "dist/smallfire.js",
  "scripts": {
    "build": "rollup -c rollup.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alan Bird",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^2.45.2",
    "rollup-plugin-terser": "^7.0.2"
  }
}